<?php
/**
 * Modules métier actifs pour ce projet. Aucun par défaut dans le starter : le cœur reste léger
 * tant qu'aucun module n'est explicitement listé ici.
 *
 * Pour activer un module fourni en exemple :
 *   return array('diagnostic');
 *
 * Le slug doit correspondre exactement à un dossier sous modules/ contenant un module.php.
 */

if (!defined('ABSPATH')) exit;

return array();
